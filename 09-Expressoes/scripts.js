// EXPRESSÕES
// códidos de uma linha difrerente de um bloco de código

const grupoA = 100;
const grupoB = 300;

const vencedor = grupoA > grupoB ? 'Grupo A venceu!' : 'Grupo B venceu'; // EXPRESSÃO
console.log(vencedor);

const areaQuadrado = (l) => l * l; // EXPRESSÃO
console.log(areaQuadrado(4));

areaQD = (l) => l * l; // EXPRESSÃO
console.log(areaQD(8));

const numeros = [1, 2, 3, 4, 5, 6];
const total = numeros.filter((numero) => numero > 4); // EXPRESSÃO
console.log(total);

const novoVencedor = grupoA > 50 && 'Grupo A Vencedor'; // EXPRESSÃO
console.log(novoVencedor);
